import fs from 'node:fs';
const path='/home/ubuntu/illustration-orbit-viewer/client/src/pages/Home.tsx'; let s=fs.readFileSync(path,'utf8');
s=s.replace('const [compareMode, setCompareMode] = useState(false);','const [compareMode, setCompareMode] = useState(false);\n  const [compareSplit, setCompareSplit] = useState(50);\n  const [draggingCompare, setDraggingCompare] = useState(false);');
s=s.replace('setParallax({ enabled: true, strength: .18 }); };','setParallax({ enabled: true, strength: .18 }); setShowThirds(false); setShowHorizon(false); setShowVanishing(false); setShowGuide(false); setCompareSplit(50); };');
s=s.replace('<div className={`viewport-wrap ${compareMode ? "is-compare" : ""}`}><div ref={mountRef} className="viewport" />{compareMode && <div className="compare-baseline">','<div className={`viewport-wrap ${compareMode ? "is-compare" : ""}`} style={{ "--compare-split": `${compareSplit}%` } as React.CSSProperties} onPointerMove={(e) => { if (!draggingCompare) return; const r = e.currentTarget.getBoundingClientRect(); setCompareSplit(Math.max(20, Math.min(80, ((e.clientX - r.left) / r.width) * 100))); }} onPointerUp={() => setDraggingCompare(false)} onPointerLeave={() => setDraggingCompare(false)}><div ref={mountRef} className="viewport" />{compareMode && <div className="compare-baseline">');
s=s.replace('<div className="compare-label"><strong>変更前</strong><span>距離 {baselineCamera.distance.toFixed(1)} · 視野角 42°</span></div><img', '<div className="compare-label"><strong>変更前</strong><span>基準画像 · 距離 {baselineCamera.distance.toFixed(1)} · 視野角 42°</span></div><div className="compare-divider" onPointerDown={(e) => { e.stopPropagation(); setDraggingCompare(true); }}><span>↔</span></div><img');
s=s.replace('<p className="panel-note camera-note">カメラ距離は被写体との位置、視野角はレンズの広さです。距離を変えず視野角だけ変えると、広角・望遠の違いを確認できます。</p>','<p className="panel-note camera-note">カメラ距離は被写体との位置、視野角はレンズの広さです。距離を変えず視野角だけ変えると、広角・望遠の違いを確認できます。</p><p className="panel-note compare-note">左右比較は、調整前の基準構図と現在の調整結果を見比べる機能です。境界線をドラッグして、確認したい側の表示幅を広げられます。</p>');
fs.writeFileSync(path,s);
