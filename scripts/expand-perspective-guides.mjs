import fs from 'node:fs';
const path='/home/ubuntu/illustration-orbit-viewer/client/src/pages/Home.tsx'; let s=fs.readFileSync(path,'utf8');
s=s.replace('const [guideWidth, setGuideWidth] = useState(2);','const [guideWidth, setGuideWidth] = useState(2);\n  const [guideCount, setGuideCount] = useState(5);\n  const [guideOpacity, setGuideOpacity] = useState(.55);');
s=s.replace('ctx.strokeStyle = "#f1ad62";', 'ctx.strokeStyle = guideColor;');
const helper='  const perimeterPoints = (count: number) => Array.from({ length: Math.max(2, count) }, (_, i) => { const t = i / (Math.max(2, count) - 1); return [{ x: t, y: 0 }, { x: 1, y: t }, { x: 1 - t, y: 1 }, { x: 0, y: 1 - t }]; }).flat();\n';
s=s.replace('  const label = camera.yaw >= 315', helper+'  const label = camera.yaw >= 315');
const old='{perspectiveMode === "1点" && ONE_POINT_EDGES.map((edge, i) => <line key={`one-${i}`} x1={edge.x} y1={edge.y} x2={vanishingPoints[0].x * 100} y2={vanishingPoints[0].y * 100} />)}{perspectiveMode !== "1点" && <>{[0,25,50,75,100].map((y) => <line key={`left-${y}`} x1="0" y1={y} x2={vanishingPoints[1].x * 100} y2={vanishingPoints[1].y * 100} />)}{[0,25,50,75,100].map((y) => <line key={`right-${y}`} x1="100" y1={y} x2={vanishingPoints[2].x * 100} y2={vanishingPoints[2].y * 100} />)}</>}{perspectiveMode === "3点" && <>{[0,25,50,75,100].map((x) => <line key={`vertical-${x}`} x1={x} y1="0" x2={vanishingPoints[0].x * 100} y2={vanishingPoints[0].y * 100} />)}</>}' ;
const replacement='{perspectiveMode === "1点" && perimeterPoints(guideCount).map((edge, i) => <line key={`one-${i}`} x1={edge.x * 100} y1={edge.y * 100} x2={vanishingPoints[0].x * 100} y2={vanishingPoints[0].y * 100} strokeOpacity={guideOpacity} />)}{perspectiveMode === "2点" && perimeterPoints(guideCount).map((edge, i) => <><line key={`left-${i}`} x1={edge.x * 100} y1={edge.y * 100} x2={vanishingPoints[1].x * 100} y2={vanishingPoints[1].y * 100} strokeOpacity={guideOpacity} /><line key={`right-${i}`} x1={edge.x * 100} y1={edge.y * 100} x2={vanishingPoints[2].x * 100} y2={vanishingPoints[2].y * 100} strokeOpacity={guideOpacity} /></>)}{perspectiveMode === "3点" && perimeterPoints(guideCount).map((edge, i) => <><line key={`left3-${i}`} x1={edge.x * 100} y1={edge.y * 100} x2={vanishingPoints[1].x * 100} y2={vanishingPoints[1].y * 100} strokeOpacity={guideOpacity} /><line key={`right3-${i}`} x1={edge.x * 100} y1={edge.y * 100} x2={vanishingPoints[2].x * 100} y2={vanishingPoints[2].y * 100} strokeOpacity={guideOpacity} /><line key={`vertical3-${i}`} x1={edge.x * 100} y1={edge.y * 100} x2={vanishingPoints[0].x * 100} y2={vanishingPoints[0].y * 100} strokeOpacity={guideOpacity} /></>)}';
if (!s.includes(old)) throw new Error('guide line anchor not found'); s=s.replace(old,replacement);
const anchor='<div className="perspective-tabs">{(["1点","2点","3点"] as PerspectiveMode[]).map((mode) => <button key={mode} className={perspectiveMode === mode ? "active" : ""} onClick={() => setPerspectiveMode(mode)}>{mode}パース</button>)}</div>';
const control=anchor+'<div className="metric-row"><span>ガイド線の本数</span><strong>{guideCount}</strong></div><Slider value={[guideCount]} min={2} max={12} step={1} onValueChange={([v]) => setGuideCount(v)} /><div className="metric-row"><span>ガイド線の透明度</span><strong>{Math.round(guideOpacity * 100)}%</strong></div><Slider value={[guideOpacity]} min={.1} max={1} step={.05} onValueChange={([v]) => setGuideOpacity(v)} />';
s=s.replace(anchor,control);
fs.writeFileSync(path,s);
